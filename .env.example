# Copy this file to .env for local development. Never commit .env.
# Use freshly rotated credentials before placing them in Render.

TELEGRAM_BOT_TOKEN=
GEMINI_API_KEY=

# Optional AI-provider fallbacks (set at least one AI key).
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# Optional outside Render or when using a custom domain.
# PUBLIC_URL=https://your-domain.example

